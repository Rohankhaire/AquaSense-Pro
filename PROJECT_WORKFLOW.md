# 🌊 AquaSense Pro - Complete Project Working Flow

## 📋 Table of Contents
1. [Project Overview](#project-overview)
2. [User Journey Flow](#user-journey-flow)
3. [System Workflow](#system-workflow)
4. [Page-by-Page Flow](#page-by-page-flow)
5. [End-to-End Scenarios](#end-to-end-scenarios)

---

## 🎯 Project Overview

**AquaSense Pro** is an AI-powered water quality monitoring platform that:
- Fetches real-time satellite data from Sentinel-2
- Uses machine learning to predict Water Quality Index (WQI)
- Provides actionable insights and recommendations
- Generates professional PDF reports

---

## 👤 User Journey Flow

```
┌─────────────────────────────────────────────────────────────────┐
│                    USER LANDS ON WEBSITE                         │
│                    http://localhost:5000                         │
└────────────────────────┬────────────────────────────────────────┘
                         │
                         ▼
        ┌────────────────────────────────────┐
        │   HOMEPAGE (Dashboard)              │
        │   • Map showing monitoring location │
        │   • Empty parameter cards           │
        │   • WQI score shows "--"            │
        │   • "Sync Satellite" button         │
        └────────────────┬───────────────────┘
                         │
                         ▼
        ┌────────────────────────────────────┐
        │   USER CLICKS "SYNC SATELLITE"     │
        └────────────────┬───────────────────┘
                         │
                         ▼
┌────────────────────────────────────────────────────────────────┐
│                    SATELLITE DATA FETCH                         │
│  1. Frontend sends GET request to /api/get-live-data           │
│  2. Backend queries Google Earth Engine                        │
│  3. Fetches Sentinel-2 imagery (last 10 days)                  │
│  4. Calculates spectral indices (NDWI, NDVI, MNDWI)            │
│  5. Converts to water parameters                               │
│  6. Returns JSON with 8 parameters                             │
└────────────────────────┬───────────────────────────────────────┘
                         │
                         ▼
        ┌────────────────────────────────────┐
        │   DASHBOARD UPDATES                 │
        │   • 8 parameter cards populated     │
        │   • Each shows value + unit         │
        │   • Color-coded (green/red)         │
        └────────────────┬───────────────────┘
                         │
                         ▼
┌────────────────────────────────────────────────────────────────┐
│                    AUTO WQI PREDICTION                          │
│  1. Frontend collects all 8 parameters                         │
│  2. Sends POST request to /api/predict                         │
│  3. Backend loads ML model (model.joblib)                      │
│  4. Runs prediction through hybrid model                       │
│  5. Returns WQI score (0-100) + assessment text                │
└────────────────────────┬───────────────────────────────────────┘
                         │
                         ▼
        ┌────────────────────────────────────┐
        │   RESULTS DISPLAYED                 │
        │   • WQI score animates to value     │
        │   • Badge shows quality level       │
        │   • Chart updates with new point    │
        │   • Recommendations appear          │
        │   • "PDF Report" button enabled     │
        └────────────────┬───────────────────┘
                         │
                         ▼
        ┌────────────────────────────────────┐
        │   USER CLICKS "PDF REPORT"         │
        │   • Downloads comprehensive report  │
        │   • Includes all parameters         │
        │   • Shows WQI and recommendations   │
        └────────────────────────────────────┘
```

---

## 🔄 System Workflow

### **Complete System Flow Diagram**

```
┌─────────────────────────────────────────────────────────────────────┐
│                         APPLICATION START                            │
│  python server.py                                                    │
└────────────────────────────┬────────────────────────────────────────┘
                             │
                             ▼
            ┌────────────────────────────────┐
            │  Flask Server Initialization   │
            │  • Load ML model (28MB)        │
            │  • Initialize Earth Engine     │
            │  • Set up routes               │
            │  • Start on port 5000          │
            └────────────────┬───────────────┘
                             │
                             ▼
            ┌────────────────────────────────┐
            │  Server Ready                  │
            │  ✓ Model loaded                │
            │  ✓ EE initialized (or LOCAL)   │
            │  ⚡ Listening on 0.0.0.0:5000  │
            └────────────────┬───────────────┘
                             │
        ┌────────────────────┴────────────────────┐
        │                                         │
        ▼                                         ▼
┌──────────────────┐                    ┌──────────────────┐
│  User Visits     │                    │  Background      │
│  Homepage        │                    │  Processes       │
└────────┬─────────┘                    └──────────────────┘
         │
         ▼
┌─────────────────────────────────────────────────────────────┐
│                    PAGE LOAD SEQUENCE                        │
│  1. Browser requests GET /                                   │
│  2. Flask serves templates/index.html                        │
│  3. Browser loads:                                           │
│     • Tailwind CSS (CDN)                                     │
│     • Chart.js (CDN)                                         │
│     • Leaflet.js (CDN)                                       │
│     • jsPDF (CDN)                                            │
│     • static/css/style.css                                   │
│     • static/js/app.js                                       │
│  4. JavaScript initializes:                                  │
│     • Leaflet map (Varanasi coordinates)                     │
│     • Chart.js (empty trend chart)                           │
│     • Event listeners                                        │
│  5. Page shows "Click 'Sync Satellite' to pull data"        │
└─────────────────────────────────────────────────────────────┘
         │
         ▼
┌─────────────────────────────────────────────────────────────┐
│              USER INTERACTION: SYNC SATELLITE                │
└────────┬────────────────────────────────────────────────────┘
         │
         ▼
┌─────────────────────────────────────────────────────────────┐
│  FRONTEND (app.js)                                           │
│  function fetchLiveData() {                                  │
│    1. Show loading message                                   │
│    2. Disable sync button                                    │
│    3. fetch('/api/get-live-data')                            │
│  }                                                           │
└────────┬────────────────────────────────────────────────────┘
         │ HTTP GET
         ▼
┌─────────────────────────────────────────────────────────────┐
│  BACKEND (server.py)                                         │
│  @app.route('/api/get-live-data')                            │
│  def get_live_data():                                        │
│    1. Check if EE_AVAILABLE                                  │
│    2. If True:                                               │
│       └─ Call get_latest_sentinel2_data()                    │
│    3. If False:                                              │
│       └─ Return PARAM_DEFAULTS                               │
│    4. Return JSON response                                   │
└────────┬────────────────────────────────────────────────────┘
         │
         ├─ IF Earth Engine Available ─────────────────────────┐
         │                                                      │
         ▼                                                      │
┌─────────────────────────────────────────────────────────────┐│
│  GOOGLE EARTH ENGINE API                                     ││
│  function get_latest_sentinel2_data():                       ││
│    1. Define date range (last 10 days)                       ││
│    2. Query Sentinel-2 collection:                           ││
│       • Filter by location (Ganga River)                     ││
│       • Filter by date                                       ││
│       • Filter clouds (<20%)                                 ││
│    3. Calculate median composite                             ││
│    4. Compute spectral indices:                              ││
│       • NDWI = (B3 - B8) / (B3 + B8)                         ││
│       • NDVI = (B8 - B4) / (B8 + B4)                         ││
│       • MNDWI = (B3 - B11) / (B3 + B11)                      ││
│    5. Extract mean values for region                         ││
│    6. Convert to water parameters:                           ││
│       • pH = 7.0 + (NDVI × 0.5)                              ││
│       • Turbidity = max(0.1, (1-NDWI) × 10)                  ││
│       • TDS = 300 + (1-MNDWI) × 200                          ││
│       • DO = 8.0 - (NDVI × 2)                                ││
│       • Temp = 25.0 (constant)                               ││
│       • Conductivity = 600 + (1-MNDWI) × 400                 ││
│       • Chlorine = 0.8 (constant)                            ││
│       • Nitrate = 5.0 + (1-NDVI) × 3                         ││
│    7. Return parameter dictionary                            ││
└─────────────────────────────────────────────────────────────┘│
         │                                                      │
         └──────────────────────────────────────────────────────┘
         │
         ▼
┌─────────────────────────────────────────────────────────────┐
│  RESPONSE TO FRONTEND                                        │
│  {                                                           │
│    "ph": 7.2,                                                │
│    "turbidity": 3.5,                                         │
│    "tds": 420,                                               │
│    "do": 7.8,                                                │
│    "temp": 25.0,                                             │
│    "conductivity": 750,                                      │
│    "chlorine": 0.8,                                          │
│    "nitrate": 6.2                                            │
│  }                                                           │
└────────┬────────────────────────────────────────────────────┘
         │
         ▼
┌─────────────────────────────────────────────────────────────┐
│  FRONTEND UPDATES UI                                         │
│  1. Create 8 parameter cards:                                │
│     ┌──────────────────┐                                     │
│     │ pH Level         │                                     │
│     │ 7.2 pH          │                                     │
│     └──────────────────┘                                     │
│  2. Store data globally: window.lastFetchedData              │
│  3. Enable "PDF Report" button                               │
│  4. Show success message in log                              │
└────────┬────────────────────────────────────────────────────┘
         │
         ▼
┌─────────────────────────────────────────────────────────────┐
│  AUTO-TRIGGER WQI PREDICTION                                 │
│  1. Collect all 8 parameters                                 │
│  2. Send POST to /api/predict                                │
└────────┬────────────────────────────────────────────────────┘
         │ HTTP POST
         ▼
┌─────────────────────────────────────────────────────────────┐
│  BACKEND ML PREDICTION                                       │
│  @app.route('/api/predict', methods=['POST'])                │
│  def predict():                                              │
│    1. Extract parameters from JSON body                      │
│    2. Validate inputs (8 required parameters)                │
│    3. Create feature vector: [ph, turb, tds, ...]            │
│    4. Load model: joblib.load('model/model.joblib')          │
│    5. Predict: wqi_raw = model.predict([features])[0]        │
│    6. Apply calibration: wqi = 0.75 × wqi_raw + 5            │
│    7. Clamp to range: wqi = max(0, min(100, wqi))            │
│    8. Get assessment: get_assessment(wqi)                    │
│    9. Return JSON response                                   │
└────────┬────────────────────────────────────────────────────┘
         │
         ▼
┌─────────────────────────────────────────────────────────────┐
│  ML MODEL INTERNALS                                          │
│  ┌─────────────────────────────────────────────────────┐    │
│  │  STACKING REGRESSOR                                  │    │
│  │  ┌──────────────────────────────────────────────┐   │    │
│  │  │  Base Model 1: Random Forest                 │   │    │
│  │  │  • 250 decision trees                        │   │    │
│  │  │  • Max depth: 12                             │   │    │
│  │  │  • Prediction: 81.5                          │   │    │
│  │  └──────────────────┬───────────────────────────┘   │    │
│  │  ┌──────────────────▼───────────────────────────┐   │    │
│  │  │  Base Model 2: XGBoost                       │   │    │
│  │  │  • 400 boosted trees                         │   │    │
│  │  │  • Learning rate: 0.05                       │   │    │
│  │  │  • Max depth: 8                              │   │    │
│  │  │  • Prediction: 79.2                          │   │    │
│  │  └──────────────────┬───────────────────────────┘   │    │
│  │  ┌──────────────────▼───────────────────────────┐   │    │
│  │  │  Meta-Learner: Linear Regression             │   │    │
│  │  │  • Combines base predictions                 │   │    │
│  │  │  • Weighted average                          │   │    │
│  │  │  • Final raw prediction: 80.3                │   │    │
│  │  └──────────────────────────────────────────────┘   │    │
│  └─────────────────────────────────────────────────────┘    │
│  Calibration: 0.75 × 80.3 + 5 = 65.2 ≈ 65                   │
└────────┬────────────────────────────────────────────────────┘
         │
         ▼
┌─────────────────────────────────────────────────────────────┐
│  RESPONSE TO FRONTEND                                        │
│  {                                                           │
│    "wqi": 65,                                                │
│    "assessment": "Fair Quality (Monitor Closely)"           │
│  }                                                           │
└────────┬────────────────────────────────────────────────────┘
         │
         ▼
┌─────────────────────────────────────────────────────────────┐
│  FRONTEND VISUAL UPDATES                                     │
│  1. Animate WQI Score:                                       │
│     • Start: 0 or previous value                             │
│     • End: 65                                                │
│     • Duration: 1500ms                                       │
│     • Effect: Counting animation                             │
│                                                              │
│  2. Update Quality Badge:                                    │
│     • Text: "Fair Quality (Monitor Closely)"                 │
│     • Color: Yellow background                               │
│     • Border: Yellow                                         │
│                                                              │
│  3. Update Border Ring:                                      │
│     • Card border: Yellow glow                               │
│                                                              │
│  4. Update Trend Chart:                                      │
│     • Add new point: (current_time, 65)                      │
│     • Keep last 10 points                                    │
│     • Smooth line animation                                  │
│                                                              │
│  5. Generate Recommendations:                                │
│     • Check each parameter vs ideal range                    │
│     • If outside range, add recommendation                   │
│     • Show recommendations panel                             │
│                                                              │
│  6. System Log:                                              │
│     • Add entry: "Analysis Complete. WQI: 65"                │
└─────────────────────────────────────────────────────────────┘
         │
         ▼
┌─────────────────────────────────────────────────────────────┐
│              USER SEES COMPLETE DASHBOARD                    │
│  ┌─────────────────────────────────────────────────────┐    │
│  │  WQI Score: 65                                       │    │
│  │  Assessment: Fair Quality (Monitor Closely)          │    │
│  │  ────────────────────────────────────────            │    │
│  │  Parameters:                                         │    │
│  │  • pH: 7.2 pH                                        │    │
│  │  • Turbidity: 3.5 NTU                                │    │
│  │  • TDS: 420 mg/L                                     │    │
│  │  • DO: 7.8 mg/L                                      │    │
│  │  • Temperature: 25.0 °C                              │    │
│  │  • Conductivity: 750 µS/cm                           │    │
│  │  • Chlorine: 0.8 mg/L                                │    │
│  │  • Nitrate: 6.2 mg/L                                 │    │
│  │  ────────────────────────────────────────            │    │
│  │  Recommendations:                                    │    │
│  │  • DO slightly low - consider aeration               │    │
│  │  ────────────────────────────────────────            │    │
│  │  [PDF Report] button enabled                         │    │
│  └─────────────────────────────────────────────────────┘    │
└─────────────────────────────────────────────────────────────┘
```

---

## 📄 Page-by-Page Flow

### **1. Homepage (Dashboard) - index.html**

```
┌─────────────────────────────────────────────────────────────┐
│  PAGE LOAD                                                   │
│  1. Render HTML structure                                    │
│  2. Initialize Leaflet map                                   │
│     • Center: Varanasi (25.3176, 82.9739)                    │
│     • Zoom: 13                                               │
│     • Marker: "Monitoring Station A1"                        │
│  3. Initialize Chart.js                                      │
│     • Type: Line chart                                       │
│     • Initial data: [70, 72, 71, 74, 73]                     │
│     • Labels: ['T-4', 'T-3', 'T-2', 'T-1', 'Start']          │
│  4. Show placeholder:                                        │
│     "Click 'Sync Satellite' to pull latest data"             │
└────────┬────────────────────────────────────────────────────┘
         │
         ▼ USER CLICKS "SYNC SATELLITE"
         │
┌────────┴────────────────────────────────────────────────────┐
│  SYNC PROCESS                                                │
│  1. fetchLiveData() called                                   │
│  2. GET /api/get-live-data                                   │
│  3. Receive 8 parameters                                     │
│  4. Create parameter cards (8 cards in grid)                 │
│  5. POST /api/predict with parameters                        │
│  6. Receive WQI + assessment                                 │
│  7. Update all UI elements                                   │
│  8. Enable "PDF Report" button                               │
└─────────────────────────────────────────────────────────────┘
         │
         ▼ USER CLICKS "PDF REPORT"
         │
┌────────┴────────────────────────────────────────────────────┐
│  PDF GENERATION                                              │
│  1. generatePDFReport() called                               │
│  2. Create jsPDF instance                                    │
│  3. Add header: "PureCast | Water Quality Report"            │
│  4. Add metadata: Date, Location                             │
│  5. Add WQI score and assessment                             │
│  6. Add parameter table (8 rows)                             │
│  7. Add recommendations (if any)                             │
│  8. Download: "PureCast_Report.pdf"                          │
└─────────────────────────────────────────────────────────────┘
```

### **2. Simulator Page - simulator.html**

```
┌─────────────────────────────────────────────────────────────┐
│  PAGE LOAD                                                   │
│  1. Render HTML structure                                    │
│  2. generateMetricInputs() called                            │
│     • Create 8 parameter controls                            │
│     • Each has: slider + number input                        │
│     • Default: mid-range values                              │
│  3. Initialize Chart.js (same as homepage)                   │
│  4. Add event listeners:                                     │
│     • Slider ↔ Input sync                                    │
│     • "Analyze Water Quality" button                         │
│     • "Sync Satellite" button (optional)                     │
└────────┬────────────────────────────────────────────────────┘
         │
         ▼ USER ADJUSTS SLIDERS
         │
┌────────┴────────────────────────────────────────────────────┐
│  REAL-TIME VALIDATION                                        │
│  1. User moves slider or types value                         │
│  2. validateRangeColor() called                              │
│  3. Check if value in ideal range                            │
│  4. Update border color:                                     │
│     • Green: Within ideal range                              │
│     • Red: Outside ideal range                               │
└─────────────────────────────────────────────────────────────┘
         │
         ▼ USER CLICKS "ANALYZE WATER QUALITY"
         │
┌────────┴────────────────────────────────────────────────────┐
│  PREDICTION PROCESS                                          │
│  1. predictQuality() called                                  │
│  2. Collect values from all 8 inputs                         │
│  3. Validate all inputs present                              │
│  4. Show loading: "Analyzing..."                             │
│  5. POST /api/predict                                        │
│  6. Receive WQI + assessment                                 │
│  7. Update UI (same as homepage)                             │
│  8. Enable "PDF Report" button                               │
└─────────────────────────────────────────────────────────────┘
         │
         ▼ OPTIONAL: USER CLICKS "SYNC SATELLITE"
         │
┌────────┴────────────────────────────────────────────────────┐
│  AUTO-FILL FROM SATELLITE                                    │
│  1. fetchLiveData() called                                   │
│  2. GET /api/get-live-data                                   │
│  3. Receive 8 parameters                                     │
│  4. Update all sliders and inputs                            │
│  5. Auto-trigger predictQuality()                            │
│  6. Show results                                             │
└─────────────────────────────────────────────────────────────┘
```

### **3. Analytics Page - analytics.html**

```
┌─────────────────────────────────────────────────────────────┐
│  PAGE LOAD                                                   │
│  1. Render HTML structure                                    │
│  2. Initialize multiple Chart.js instances:                  │
│     • Historical WQI trend (line chart)                      │
│     • Parameter distribution (bar chart)                     │
│     • Correlation matrix (heatmap)                           │
│  3. Load historical data (if available)                      │
│  4. Display statistics and insights                          │
└─────────────────────────────────────────────────────────────┘
```

---

## 🎬 End-to-End Scenarios

### **Scenario 1: First-Time User**

```
1. User opens http://localhost:5000
   ↓
2. Sees beautiful dashboard with map
   ↓
3. Reads: "Click 'Sync Satellite' to pull latest data"
   ↓
4. Clicks blue "Sync Satellite" button
   ↓
5. Sees loading message: "Contacting Sentinel-2 Satellite API..."
   ↓
6. After 2-5 seconds, 8 parameter cards appear with values
   ↓
7. WQI score animates from 0 to calculated value (e.g., 78)
   ↓
8. Badge shows: "Good Quality (Minor Issues)" in teal
   ↓
9. Chart updates with new data point
   ↓
10. Recommendations appear (if any issues detected)
    ↓
11. "PDF Report" button appears in header
    ↓
12. User clicks "PDF Report"
    ↓
13. Downloads comprehensive PDF with all data
```

### **Scenario 2: Manual Testing (Simulator)**

```
1. User navigates to /simulator
   ↓
2. Sees 8 parameter sliders with default mid-range values
   ↓
3. Adjusts pH slider from 7.0 to 5.5 (acidic)
   ↓
4. Input border turns red (outside ideal range)
   ↓
5. Adjusts turbidity to 15 NTU (high)
   ↓
6. Input border turns red
   ↓
7. Clicks "Analyze Water Quality"
   ↓
8. Button shows spinning loader: "Analyzing..."
   ↓
9. WQI calculated: 42 (Poor Quality)
   ↓
10. Badge shows: "Poor Quality (Immediate Concern)" in red
    ↓
11. Recommendations panel appears:
    • "Increase alkalinity"
    • "High suspension. Filtration required."
    • "WQI is Critical. Immediate water treatment recommended."
    ↓
12. User can download PDF or adjust parameters again
```

### **Scenario 3: Monitoring Over Time**

```
1. User syncs satellite data at 9:00 AM
   → WQI: 75 (Good)
   ↓
2. Chart shows point at 9:00 AM with value 75
   ↓
3. User syncs again at 12:00 PM
   → WQI: 68 (Fair)
   ↓
4. Chart now shows two points with downward trend
   ↓
5. User syncs at 3:00 PM
   → WQI: 62 (Fair)
   ↓
6. Chart shows declining trend over 6 hours
   ↓
7. User notices pattern and takes action
   ↓
8. Downloads PDF report for documentation
```

---

## 🔁 Continuous Operation Flow

```
┌─────────────────────────────────────────────────────────────┐
│  SERVER RUNNING (python server.py)                          │
│  ┌─────────────────────────────────────────────────────┐    │
│  │  Flask App Loop                                      │    │
│  │  • Listen for HTTP requests                          │    │
│  │  • Route to appropriate handler                      │    │
│  │  • Return responses                                  │    │
│  └─────────────────────────────────────────────────────┘    │
│                                                              │
│  ┌─────────────────────────────────────────────────────┐    │
│  │  ML Model (Loaded in Memory)                         │    │
│  │  • 28.8 MB joblib file                               │    │
│  │  • Ready for instant predictions                     │    │
│  │  • No re-training needed                             │    │
│  └─────────────────────────────────────────────────────┘    │
│                                                              │
│  ┌─────────────────────────────────────────────────────┐    │
│  │  Earth Engine Connection                             │    │
│  │  • Authenticated session                             │    │
│  │  • Ready for satellite queries                       │    │
│  │  • Falls back to LOCAL mode if unavailable          │    │
│  └─────────────────────────────────────────────────────┘    │
└─────────────────────────────────────────────────────────────┘
         │
         │ Multiple users can connect simultaneously
         │
    ┌────┴────┬────────┬────────┐
    │         │        │        │
    ▼         ▼        ▼        ▼
  User 1   User 2   User 3   User N
  (Home)   (Sim)    (Analytics) (Home)
```

---

## 📊 Data Persistence

```
┌─────────────────────────────────────────────────────────────┐
│  CURRENT STATE (No Database)                                 │
│  • Each session is independent                               │
│  • Chart data stored in browser memory                       │
│  • Refreshing page clears history                            │
│  • No user accounts or saved reports                         │
└─────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│  FUTURE ENHANCEMENT (With Database)                          │
│  • PostgreSQL for historical data                            │
│  • User authentication                                       │
│  • Saved reports and trends                                  │
│  • Multi-location monitoring                                 │
│  • Alert notifications                                       │
└─────────────────────────────────────────────────────────────┘
```

---

## 🎯 Key Takeaways

### **What Makes This System Work:**

1. **Real-time Satellite Integration**
   - Sentinel-2 provides actual environmental data
   - 10-day averaging reduces noise
   - Spectral indices correlate with water quality

2. **Hybrid ML Model**
   - Combines Random Forest (stability) + XGBoost (accuracy)
   - Stacking meta-learner optimizes predictions
   - Calibration ensures realistic results

3. **User-Friendly Interface**
   - Manual sync gives user control
   - Visual feedback (colors, animations)
   - Instant results (<50ms prediction)
   - Professional PDF reports

4. **Flexible Architecture**
   - Works with or without Earth Engine
   - Simulator for testing scenarios
   - Analytics for trend analysis
   - Modular design for easy expansion

---

## 🚀 Quick Start Summary

```bash
# 1. Install dependencies
pip install -r requirements.txt

# 2. Train model (one-time)
python train_model.py

# 3. Run server
python server.py

# 4. Open browser
http://localhost:5000

# 5. Click "Sync Satellite"
# 6. View results
# 7. Download PDF
```

---

**Last Updated:** February 4, 2026  
**Version:** 1.0.0  
**Status:** Production Ready ✅
